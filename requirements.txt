# ===========================================
# Multi-Agent Planner - Dependencies
# ===========================================

# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.28.0
tqdm>=4.65.0

# LLM Provider - Groq (FREE - Default)
groq>=1.0.0

# Optional: Additional LLM Providers
# Uncomment the ones you need:

# google-generativeai>=0.8.0    # Google Gemini (FREE tier)
# openai>=1.0.0                 # OpenAI (Paid)

# Note: Ollama uses HTTP requests, no extra package needed
# Note: OpenRouter uses the openai package with custom base_url
